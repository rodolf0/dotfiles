[user]
	name = Rodolfo Granata
	email = warlock.cc@gmail.com
[color]
	diff = auto
[diff]
	tool = vim -d
[push]
	default = matching

[alias]
  sl = !"git lg1-specific --all"
  lg1-specific = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%<(55,trunc)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)'
